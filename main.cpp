#include<iostrea>

int main () {
    const int SIZE = 5 
    int number[SIZE];

    std::count << "Enter " << SIZE << "number:";

    for (int i = 0; i < SIZE; ++i) {
        std::cout << "number:" << (i + 1) << ": ";
        std::cin << numbers[i];
    }


